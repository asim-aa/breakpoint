[
  {
    "id": "merge_intervals",
    "difficulty": "easy",
    "request": "merge overlapping intervals in a list of [start, end] pairs"
  },
  {
    "id": "first_last_index",
    "difficulty": "boundary-heavy",
    "request": "given a sorted array of integers, find the first and last index of a target value, returning [-1, -1] if the target is not found"
  },
  {
    "id": "two_sum_indices",
    "difficulty": "spec-ambiguous",
    "request": "given a list of integers, return the two indices whose values sum to a given target, or None if no pair exists"
  },
  {
    "id": "csv_row_parse",
    "difficulty": "boundary-heavy",
    "request": "parse a CSV row into a list of fields, respecting double-quoted fields that may contain commas"
  },
  {
    "id": "reverse_words_preserve_whitespace",
    "difficulty": "spec-ambiguous",
    "request": "reverse the words in a sentence while preserving the whitespace between them"
  },
  {
    "id": "valid_palindrome",
    "difficulty": "moderate",
    "request": "check whether a string is a valid palindrome, ignoring case, spaces, and punctuation"
  },
  {
    "id": "dedupe_preserve_order",
    "difficulty": "easy",
    "request": "remove duplicate values from a list while preserving the order of their first occurrence"
  },
  {
    "id": "merge_two_sorted_arrays",
    "difficulty": "boundary-heavy",
    "request": "merge two sorted lists of integers into a single sorted list"
  },
  {
    "id": "first_non_repeating_char",
    "difficulty": "spec-ambiguous",
    "request": "find the first character in a string that does not repeat, or return None if every character repeats"
  },
  {
    "id": "max_profit_single_trade",
    "difficulty": "moderate",
    "request": "given a list of daily stock prices, find the maximum profit achievable from a single buy followed by a later sell"
  }
]
